import blocks from './content.js'

export default {
  id: 'article03',
  blocks,
  theme: {
    background: 'var(--color-blue)',
    padding: '32px',
  },
  layout: {
    width: 450,
    leftOffset: 24,
    topOffset: -24,
    caption: 'Chapter three — written after the room got quiet and the notes were still on the table.',
  },
}
