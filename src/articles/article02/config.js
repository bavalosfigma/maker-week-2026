import blocks from './content.js'

export default {
  id: 'article02',
  blocks,
  theme: {
    background: 'var(--color-green)',
    padding: 'clamp(1.5rem, 4vw, 2.75rem)',
  },
  layout: {
    width: 600,
  },
}
