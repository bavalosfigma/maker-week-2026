import blocks from './content.js'

export default {
  id: 'article01',
  blocks,
  theme: {
    background: 'var(--color-yellow)',
    padding: 'clamp(1.5rem, 4vw, 2.75rem)',
  },
  layout: {
    width: 800,
  },
}
