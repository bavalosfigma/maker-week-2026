import blocks from './content.js'

export default {
  id: 'article04',
  blocks,
  theme: {
    background: 'var(--color-white)',
    padding: '32px',
  },
  layout: {
    width: 450,
    top: 0.18,
    leftOffset: 96,
    caption: 'Chapter four — assembled from the tests that were never meant to be shown.',
  },
}
